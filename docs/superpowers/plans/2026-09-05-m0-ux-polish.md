# M0 UX 打磨实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付三项 UX 增强——单手落子节奏、胜利金色脉冲特效、终局 30s 重开提示条。

**Architecture:** 后端 `/api/move` 单手化（AI 应手统一走 `/api/ai-move`），`Board.winning_line()` + `_state.win_line` 支撑前端特效；前端 App 保持单循环，Board 组件加 winLine 渲染，CSS 动画无新依赖。

**Tech Stack:** 既有栈不变（FastAPI/React/纯 CSS 动画）。

**Spec:** `docs/superpowers/specs/2026-09-05-m0-ux-polish-design.md`

## Global Constraints

- `/api/move` 只走人这一手；AI 落子唯一入口是 `/api/ai-move`（`/api/new` 黑 AI 首手除外）
- `_state` 新字段名 `win_line`，值 `list[list[int]] | None`；未终局/平局为 None
- `Board.winning_line()` 返回 `list[tuple[int, int]] | None`，含最后一手的连续 5 格
- 特效：金色脉冲（drop-shadow 呼吸 + scale 1→1.15），animation-delay = 序号 × 0.15s，其余棋子 opacity 0.45；纯 CSS，不加依赖
- 提示条：终局（含平局）30s 后出现；【重开一局】回配置页 /【继续看】关闭且不再弹
- 分支 feature/m0，逐任务 commit，不 push（push 留给 M0 收尾任务）
- Windows + bash，路径正斜杠

---

### Task 1: Board.winning_line()

**Files:**
- Modify: `src/gomoku/core.py`（Board 类内、`_has_five` 之后追加方法）
- Test: `tests/test_core.py`（文件末尾追加）

**Interfaces:**
- Consumes: Task 2(M0) 的 `_DIRS/DRAW/last_move/winner/game_over`
- Produces: `Board.winning_line() -> list[tuple[int, int]] | None`——Task 2（本计划）的 server 依赖

- [ ] **Step 1: 追加失败测试**

在 `tests/test_core.py` 末尾追加：

```python
def test_winning_line_horizontal():
    b = Board()
    for r, c in [(7, 4), (8, 0), (7, 5), (8, 1), (7, 6), (8, 2), (7, 7), (8, 3), (7, 8)]:
        b.play(r, c)
    line = b.winning_line()
    assert line is not None and len(line) == 5
    assert (7, 8) in line and (7, 4) in line
    assert all(r == 7 for r, _ in line)


def test_winning_line_none_midgame():
    b = Board()
    b.play(7, 7)
    b.play(7, 8)
    assert b.winning_line() is None
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/test_core.py -q`
Expected: FAIL（`AttributeError: 'Board' object has no attribute 'winning_line'`，2 failed）

- [ ] **Step 3: 写实现**

在 `src/gomoku/core.py` 的 Board 类中、`_has_five` 方法定义之后追加：

```python
    def winning_line(self) -> list[tuple[int, int]] | None:
        """获胜五连坐标（含最后一手的一段 5 格）；未终局或平局返回 None"""
        if not self.game_over or self.winner == DRAW:
            return None
        row, col = self.last_move
        player = self.winner
        for dr, dc in _DIRS:
            cells = [(row, col)]
            for sign in (1, -1):
                r, c = row + sign * dr, col + sign * dc
                while 0 <= r < self.size and 0 <= c < self.size \
                        and self.grid[r, c] == player:
                    cells.append((r, c))
                    r += sign * dr
                    c += sign * dc
            if len(cells) >= 5:
                return cells[:5]
        return None
```

（cells 首元素是最后一手，沿正方向延伸在前——返回段有序，前端按序依次点亮。）

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/test_core.py -q`
Expected: `13 passed`

- [ ] **Step 5: Commit**

```bash
git add src/gomoku/core.py tests/test_core.py
git commit -m "feat: Board.winning_line 获胜五连坐标"
```

---

### Task 2: 后端单手端点 + win_line 字段

**Files:**
- Modify: `src/gomoku/server/app.py`（`_state` 与 `move` 端点两处）
- Test: `tests/test_server.py`（替换 1 个测试、追加 1 个）

**Interfaces:**
- Consumes: Task 1 `Board.winning_line()`；M0 Task 7 的 `_play_one_ai_move/_maybe_finish/_state`
- Produces: `/api/move` 单手语义；状态 JSON 新增 `win_line`——前端（Task 3）依赖

- [ ] **Step 1: 更新测试**

`tests/test_server.py` 中删除整个 `test_human_move_then_ai_reply`，原位替换为：

```python
def test_human_move_single_ply():
    r = client.post("/api/new", json={"black": "human", "white": "minimax-easy"})
    gid = r.json()["game_id"]
    r = client.post("/api/move", json={"game_id": gid, "row": 7, "col": 7})
    assert r.status_code == 200
    s = r.json()
    assert s["board"][7][7] == 1
    assert len(s["moves"]) == 1            # 只走人这一手
    assert s["current_player"] == 2
    # AI 应手改由 /api/ai-move 驱动
    s = client.post("/api/ai-move", json={"game_id": gid}).json()
    assert len(s["moves"]) == 2 and s["current_player"] == 1


def test_win_line_field():
    r = client.post("/api/new", json={"black": "human", "white": "human"})
    gid = r.json()["game_id"]
    for row, col in [(7, 4), (8, 0), (7, 5), (8, 1), (7, 6), (8, 2), (7, 7), (8, 3), (7, 8)]:
        s = client.post("/api/move", json={"game_id": gid, "row": row, "col": col}).json()
    assert s["game_over"] and s["winner"] == 1
    assert len(s["win_line"]) == 5
    assert [7, 8] in s["win_line"] and [7, 4] in s["win_line"]
```

- [ ] **Step 2: 运行确认失败**

Run: `pytest tests/test_server.py -q`
Expected: FAIL（`test_human_move_single_ply` 中 `len(s["moves"]) == 1` 不成立——现在仍返回两手）

- [ ] **Step 3: 改 app.py 两处**

`_state` 函数改为（新增 win_line 行）：

```python
def _state(game_id: int) -> dict:
    s = sessions[game_id]
    b = s["board"]
    line = b.winning_line()
    return {
        "game_id": game_id,
        "board": b.grid.tolist(),
        "current_player": b.current_player,
        "last_move": list(b.last_move) if b.last_move else None,
        "winner": b.winner,
        "game_over": b.game_over,
        "win_line": [[r, c] for r, c in line] if line else None,
        "moves": [[p, r, c] for p, r, c in b.history],
    }
```

`move` 端点末尾改为（删除 AI 应手调用）：

```python
    record_move(conn, req.game_id, board.history[-1])
    _maybe_finish(req.game_id)
    return _state(req.game_id)
```

（`_play_one_ai_move` 保留——`/api/new` 与 `/api/ai-move` 仍在用。）

- [ ] **Step 4: 运行确认通过**

Run: `pytest tests/test_server.py -q && pytest -q`
Expected: server 7 passed；全量 37 passed

- [ ] **Step 5: Commit**

```bash
git add src/gomoku/server/app.py tests/test_server.py
git commit -m "feat: /api/move 单手化 + win_line 字段"
```

---

### Task 3: 前端单手循环 + 金色脉冲特效 + 30s 提示条

**Files:**
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/Board.jsx`
- Modify: `frontend/src/index.css`（末尾追加）

**Interfaces:**
- Consumes: Task 2 的 `win_line` 字段；Task 9(M0) 的 App/Board 现有结构、api.js（不改动）
- Produces: 三项 UX 行为（浏览器验收对象）

- [ ] **Step 1: App.jsx 改三处**

① state 区（`const [toast, setToast] = useState("");` 之后）加：

```jsx
  const [showRematch, setShowRematch] = useState(false);
```

② useEffect 区（AI 回合 effect 之后）加终局 30s 提示 effect：

```jsx
  // 终局 30 秒后提示重开（含平局）
  useEffect(() => {
    if (!game?.game_over) { setShowRematch(false); return; }
    const t = setTimeout(() => setShowRematch(true), 30_000);
    return () => clearTimeout(t);
  }, [game]);
```

③ return 的对局页 JSX：Board 调用加 winLine prop，Board 之后、toolbar 之前加提示条：

```jsx
      <Board grid={game.board} lastMove={game.last_move} winLine={game.win_line}
             onPlay={play} locked={busy || game.game_over || !humanTurn} />
      {game.game_over && showRematch && (
        <div className="rematch-bar">
          <span>再来一局？</span>
          <button onClick={() => setGame(null)}>重开一局</button>
          <button onClick={() => setShowRematch(false)}>继续看</button>
        </div>
      )}
      <div className="toolbar">
```

- [ ] **Step 2: Board.jsx 支持 winLine**

组件签名与 win 索引（`export default function Board(...)` 行改为）：

```jsx
export default function Board({ grid, lastMove, winLine, onPlay, locked }) {
  const winIndex = new Map();
  if (winLine) {
    winLine.forEach(([r, c], i) => winIndex.set(r + "-" + c, i));
  }
```

棋子渲染（grid.map 里的 circle）改为：

```jsx
      {grid.map((row, r) =>
        row.map((v, c) =>
          v === 0 ? null : (
            (() => {
              const key = r + "-" + c;
              const winIdx = winIndex.get(key);
              return (
                <circle key={key} cx={PAD + c * CELL} cy={PAD + r * CELL}
                        r={CELL * 0.42}
                        className={winIdx !== undefined ? "stone-win" : undefined}
                        opacity={winIndex.size && winIdx === undefined ? 0.45 : 1}
                        style={winIdx !== undefined
                          ? { animationDelay: `${winIdx * 0.15}s` } : undefined}
                        fill={v === 1 ? "#111" : "#fff"} stroke="#555" />
              );
            })()
          )
        )
      )}
```

- [ ] **Step 3: index.css 末尾追加特效与提示条样式**

```css
.stone-win {
  animation: winPulse 1.2s ease-in-out infinite;
  transform-box: fill-box;
  transform-origin: center;
  filter: drop-shadow(0 0 6px #ffd700);
}
@keyframes winPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.15); }
}
.rematch-bar {
  margin-top: 14px;
  display: flex;
  gap: 12px;
  align-items: center;
  justify-content: center;
  padding: 10px 16px;
  background: rgba(255, 215, 0, 0.12);
  border: 1px solid #ffd700;
  border-radius: 8px;
}
```

- [ ] **Step 4: 构建验证**

Run: `cd frontend && npm run build`
Expected: 构建成功无报错

- [ ] **Step 5: Commit**

```bash
git add frontend/src/App.jsx frontend/src/Board.jsx frontend/src/index.css
git commit -m "feat: 单手落子节奏 + 金色脉冲胜利特效 + 30s 重开提示条"
```

---

### Task 4: 全量回归 + 浏览器人工验收

**Files:** 无新改动（验证任务）

- [ ] **Step 1: 全量测试**

Run: `pytest -q`
Expected: 37 passed

- [ ] **Step 2: 生产模式起服**

```bash
cd frontend && npm run build && cd ..
uvicorn gomoku.server.app:app --port 8000
```

浏览器打开 `http://127.0.0.1:8000`。

- [ ] **Step 3: 人工验收三点**

1. 人机局：点击落子 → 玩家棋子**立即**出现 → 约 0.4s+AI 思考时间后 AI 棋子单独出现（"思考中…"指示期间锁盘）
2. 下到五连：获胜五子金色光晕脉冲、依次点亮、其余棋子变暗
3. 终局 30s 后提示条出现：【继续看】关闭不再弹；【重开一局】回配置页；平局时同样有提示条、无特效

- [ ] **Step 4: 验收结果回传控制者**（决定 M0 收尾：README 勾选 / push / 最终审查）

## Self-Review 记录

1. **Spec 覆盖**：单手端点→Task 2；winning_line/win_line→Task 1/2；金色脉冲→Task 3；30s 提示条→Task 3；验收→Task 4。spec"明确不做"均未纳入。✓
2. **占位扫描**：全部步骤含完整代码/命令。✓
3. **类型一致性**：`winning_line()` 返回元组列表、`win_line` JSON 数组数组；前端 `winLine` prop 与 App 传参、Board 解构一致；`_play_one_ai_move` 保留给 new/ai-move 两端点。✓
