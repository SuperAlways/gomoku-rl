# E0 表格 Q-learning（3×3）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 3×3 连三制棋盘上跑通表格式 Q-learning 整条环路（ε-greedy / TD 更新 / 稀疏奖励回填），训练产物通过 QTablePlayer 接入现有 Web UI 对战。

**Architecture:** `Board` 泛化为 `(size, win_len)`（默认行为不变）；Q-learning 核心做成可测库 `src/gomoku/rl/tabular.py`，`scripts/train_tabular.py` / `scripts/plot_qtable.py` 是薄 CLI；`QTablePlayer` 实现 `Player` 接口加载 qtable.json；server 透传棋盘尺寸并加 `qtable` 玩家分支；前端 `Board.jsx` 参数化尺寸 + 配置页「九宫格·Q表」入口。

**Tech Stack:** Python 3.11 / numpy / FastAPI（现有）；matplotlib（新增，仅曲线与热力图，Agg 后端）；React + Vite（现有，不新增依赖）。

**Spec:** `docs/superpowers/specs/2026-09-05-e0-tabular-q-design.md`

## Global Constraints

- 从 `main` 拉出分支 `feature/e0`，全部工作在该分支上提交。
- 每个 Python 任务结束时全量 `python -m pytest` 必须通过（现有 47 个测试 + 新增）。
- 前端任务结束时 `cd frontend && npm run build` 必须通过。
- 训练/绘图脚本必须 headless 可用：matplotlib 用 `Agg` 后端，脚本内 `matplotlib.use("Agg")` 在 `pyplot` 导入之前。
- Q 表 JSON 格式固定：`{"meta": {...}, "states": {"<base64(state_key)>": [size*size 个 float]}}`；`state_key = board.grid.tobytes() + bytes([board.current_player])`。
- 奖励约定（spec §2）：胜 +1 / 负 -1 / 平 0，只回填 Q 学员（执黑）的着。
- TD 更新（spec §2）：`Q[s][a] += lr * (r + γ * max_a' Q[s'][a'] * (1-done) - Q[s][a])`，episode 内逐手反向；bootstrap 的 max 只在**合法动作**上取。
- 已验证的阈值（seed=0 实测）：smoke 训练 500 局 vs random 最后 100 局胜率 ≈ 0.75；stage1（5000 局）≈ 0.97；stage2（10000 局 vs blocker）≈ 0.89。
- Windows + bash 环境；pytest 从仓库根目录跑。

---

### Task 1: Board 泛化 `(size=15, win_len=5)`

**Files:**
- Modify: `src/gomoku/core.py`
- Test: `tests/test_core.py`（追加）

**Interfaces:**
- Consumes: 现有 `Board`（`core.py:12-106`）。
- Produces: `Board(size: int = BOARD_SIZE, win_len: int = 5)`，属性 `self.win_len`；私有方法 `_has_line(row, col, player)`（替代 `_has_five`）；`winning_line()` 阈值随 `win_len`。后续所有任务依赖此签名。

- [ ] **Step 1: 写失败测试**

在 `tests/test_core.py` 末尾追加：

```python
def test_3x3_horizontal_win():
    b = Board(size=3, win_len=3)
    assert b.win_len == 3
    for r, c in [(0, 0), (1, 0), (0, 1), (1, 1), (0, 2)]:
        b.play(r, c)
    assert b.winner == BLACK and b.game_over
    line = b.winning_line()
    assert line is not None and len(line) == 3
    assert all(r == 0 for r, _ in line)


def test_3x3_draw_full_board():
    # 经典和棋谱：X O X / O X X / O X O 的变体——黑白各无三连
    b = Board(size=3, win_len=3)
    for r, c in [(0, 0), (0, 1), (0, 2), (1, 1), (1, 0), (1, 2), (2, 1), (2, 0), (2, 2)]:
        b.play(r, c)
    assert b.winner == DRAW and len(b.history) == 9
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_core.py -v`
Expected: 新增两测试 FAIL，报 `TypeError: __init__() got an unexpected keyword argument 'win_len'`

- [ ] **Step 3: 实现**

`src/gomoku/core.py` 改动（其余不动）：

类 docstring 第一行改为：

```python
    """无禁手五子棋棋盘（默认 15×15 连五，可缩小为 3×3 连三等）：规则内核，唯一的状态权威。
```

`__init__` 改为：

```python
    def __init__(self, size: int = BOARD_SIZE, win_len: int = 5):
        self.size = size
        self.win_len = win_len
        self.grid = np.zeros((size, size), dtype=np.int8)
        self.current_player = BLACK
        self.winner = EMPTY
        self.history: list[tuple[int, int, int]] = []
        self.last_move: tuple[int, int] | None = None
```

`play()` 中 `if self._has_five(row, col, player):` 改为 `if self._has_line(row, col, player):`

`_has_five` 重命名并泛化（只改方法名与阈值）：

```python
    def _has_line(self, row: int, col: int, player: int) -> bool:
        # 只以最后一手为中心查 4 方向，不做全盘扫描
        for dr, dc in _DIRS:
            count = 1
            for sign in (1, -1):
                r, c = row + sign * dr, col + sign * dc
                while 0 <= r < self.size and 0 <= c < self.size and self.grid[r, c] == player:
                    count += 1
                    r += sign * dr
                    c += sign * dc
            if count >= self.win_len:
                return True
        return False
```

`winning_line()` 中 `if len(cells) >= 5:` 改为 `if len(cells) >= self.win_len:`，`return cells[:5]` 改为 `return cells[:self.win_len]`

- [ ] **Step 4: 全量测试通过**

Run: `python -m pytest`
Expected: 49 passed（47 旧 + 2 新）

- [ ] **Step 5: Commit**

```bash
git add src/gomoku/core.py tests/test_core.py
git commit -m "feat: Board 泛化 (size, win_len)，默认行为不变"
```

---

### Task 2: 表格 Q-learning 库 + 训练/绘图脚本

**Files:**
- Create: `src/gomoku/rl/__init__.py`（空文件）
- Create: `src/gomoku/rl/tabular.py`
- Create: `scripts/train_tabular.py`
- Create: `scripts/plot_qtable.py`
- Modify: `pyproject.toml`（dependencies 加 matplotlib）
- Test: `tests/test_tabular.py`

**Interfaces:**
- Consumes: `Board(size, win_len)`（Task 1）、`gomoku.core._DIRS`、`BLACK/WHITE/DRAW`。
- Produces（Task 3/4/6 依赖）:
  - `state_key(board: Board) -> bytes`
  - `OPPONENTS: dict[str, callable]`，元素签名 `(board, rng) -> int`
  - `train(opponent: str, episodes: int, eps_start: float, eps_end: float, eps_decay_episodes: int, gamma: float, lr: float, seed: int | None, size: int, win_len: int) -> tuple[dict[bytes, np.ndarray], list[dict]]`
  - `save_qtable(q: dict, path: str, meta: dict) -> None` / `load_qtable(path: str) -> tuple[dict[bytes, np.ndarray], dict]`
  - `sliding_win_rate(rows: list[dict], window: int = 100) -> list[float]`
  - metrics 行格式：`{"episode": int, "winner": int, "steps": int, "td_error": float, "entropy": float, "eps": float}`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_tabular.py`：

```python
import numpy as np
import pytest

from gomoku.core import Board, BLACK
from gomoku.rl.tabular import (
    blocker_opponent,
    load_qtable,
    policy_entropy,
    save_qtable,
    select_action,
    sliding_win_rate,
    state_key,
    td_backfill,
    train,
)


def test_state_key_distinguishes_player():
    grid = np.zeros((3, 3), np.int8)
    grid[0, 0] = BLACK
    grid[1, 1] = 2
    key_black = grid.tobytes() + bytes([BLACK])
    key_white = grid.tobytes() + bytes([2])
    assert key_black != key_white
    b = Board(size=3, win_len=3)
    b.play(0, 0)
    b.play(1, 1)          # 两手后轮回黑方，grid 与 key_black 一致
    assert state_key(b) == key_black


def test_blocker_blocks_threat():
    b = Board(size=3, win_len=3)
    b.play(0, 0)          # 黑
    b.play(2, 2)          # 白（blocker 方）
    b.play(0, 1)          # 黑成两连
    assert b.current_player == 2
    a = blocker_opponent(b, np.random.default_rng(0))
    assert b.action_to_move(a) == (0, 2)


def test_blocker_random_fallback():
    b = Board(size=3, win_len=3)
    a = blocker_opponent(b, np.random.default_rng(0))
    r, c = b.action_to_move(a)
    assert b.grid[r, c] == 0


def test_select_action_argmax_masks_occupied():
    b = Board(size=3, win_len=3)
    b.play(1, 1)          # 黑
    b.play(0, 0)          # 白
    key = state_key(b)
    q = {key: np.arange(9, dtype=float)}
    a = select_action(q, b, eps=0.0, rng=np.random.default_rng(0))
    assert b.action_to_move(a) == (2, 2)      # (0,0) 已占被 mask，argmax=8


def test_select_action_unknown_state_random_legal():
    b = Board(size=3, win_len=3)
    a = select_action({}, b, eps=0.0, rng=np.random.default_rng(0))
    assert 0 <= a < 9


def test_policy_entropy_uniform_and_peaked():
    legal = np.arange(9)
    assert policy_entropy(np.zeros(9), legal) == pytest.approx(np.log(9), rel=1e-3)
    peaked = np.zeros(9)
    peaked[4] = 10.0
    assert policy_entropy(peaked, legal) < 0.1


def test_td_backfill_values():
    s = np.zeros((3, 3), np.int8).tobytes() + bytes([BLACK])
    q = {}
    errs = td_backfill(q, [(s, 0), (s, 8)], reward=1.0, gamma=0.99, lr=0.5)
    assert q[s][8] == pytest.approx(0.5)                  # 终局手：0 + 0.5*(1-0)
    assert q[s][0] == pytest.approx(0.5 * 0.99 * 0.5)     # γ·max(合法 Q[s']) = 0.495
    assert errs == [pytest.approx(1.0), pytest.approx(1.0)]


def test_train_smoke_beats_random():
    # seed=0 实测最后 100 局胜率 ≈ 0.75，阈值留足余量
    q, rows = train(opponent="random", episodes=500, eps_start=1.0,
                    eps_end=0.1, eps_decay_episodes=300, gamma=0.99,
                    lr=0.5, seed=0)
    assert len(rows) == 500 and len(q) > 0
    assert rows[0]["episode"] == 0
    assert sliding_win_rate(rows)[-1] > 0.6


def test_save_load_roundtrip(tmp_path):
    q = {b"\x00" * 9 + b"\x01": np.arange(9, dtype=float)}
    path = tmp_path / "qtable.json"
    save_qtable(q, str(path), meta={"size": 3})
    q2, meta = load_qtable(str(path))
    assert meta["size"] == 3
    key = b"\x00" * 9 + b"\x01"
    assert np.allclose(q2[key], np.arange(9))


def test_sliding_win_rate():
    rows = [{"winner": 1}] * 150
    rates = sliding_win_rate(rows, window=100)
    assert len(rates) == 150 and rates[0] == 1.0 and rates[99] == 1.0
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_tabular.py -v`
Expected: 全部 FAIL，`ModuleNotFoundError: No module named 'gomoku.rl'`

- [ ] **Step 3: 实现库**

创建 `src/gomoku/rl/__init__.py`（空文件）。

创建 `src/gomoku/rl/tabular.py`：

```python
"""表格 Q-learning（3×3 连三制，课 01 E0）。

状态键 = grid.tobytes() + bytes([current_player])：同一局面按行棋方区分。
Q 表 = dict[bytes, np.ndarray(size*size)]，初始全 0；合法动作按棋盘实况 mask。
"""

import base64
import json

import numpy as np

from gomoku.core import Board, BLACK, WHITE, DRAW, _DIRS

SIZE = 3
WIN_LEN = 3


def state_key(board: Board) -> bytes:
    return board.grid.tobytes() + bytes([board.current_player])


def _legal_actions(board: Board) -> np.ndarray:
    return np.flatnonzero(board.valid_moves())


def select_action(q: dict, board: Board, eps: float,
                  rng: np.random.Generator) -> int:
    """ε-greedy：以 eps 随机走合法位，否则查表 argmax（查不到的局面→随机）。"""
    legal = _legal_actions(board)
    if rng.random() < eps:
        return int(rng.choice(legal))
    key = state_key(board)
    if key not in q:
        return int(rng.choice(legal))
    return int(legal[np.argmax(q[key][legal])])


def random_opponent(board: Board, rng: np.random.Generator) -> int:
    return int(rng.choice(_legal_actions(board)))


def _completes_line(board: Board, row: int, col: int, player: int) -> bool:
    """假设 player 落 (row,col) 是否成 win_len 连（不改动棋盘）。"""
    for dr, dc in _DIRS:
        count = 1
        for sign in (1, -1):
            r, c = row + sign * dr, col + sign * dc
            while 0 <= r < board.size and 0 <= c < board.size \
                    and board.grid[r, c] == player:
                count += 1
                r += sign * dr
                c += sign * dc
        if count >= board.win_len:
            return True
    return False


def blocker_opponent(board: Board, rng: np.random.Generator) -> int:
    """贪心脚本：对方（学员）两连则必堵，否则随机。固定不可学。"""
    legal = _legal_actions(board)
    threat = 3 - board.current_player
    for a in legal:
        r, c = board.action_to_move(int(a))
        if _completes_line(board, r, c, threat):
            return int(a)
    return int(rng.choice(legal))


OPPONENTS = {"random": random_opponent, "blocker": blocker_opponent}


def td_backfill(q: dict, trajectory: list[tuple[bytes, int]], reward: float,
                gamma: float, lr: float) -> list[float]:
    """终局奖励反向逐手回填学员的着；返回各手 TD error（更新前）。

    最后一手 done=1：target = reward；其余 target = γ·max_{合法 a'} Q[s'][a']。
    反向遍历时 Q[s'][a'] 恰是上一轮刚更新过的下一决策状态。
    合法性从状态键解码棋盘得出——从未更新过的非法位（Q=0）不许污染 bootstrap。
    """
    errs = []
    target = reward
    for key, action in reversed(trajectory):
        errs.append(abs(target - q[key][action]))
        q[key][action] += lr * (target - q[key][action])
        grid = np.frombuffer(key[:-1], dtype=np.int8)
        legal = np.flatnonzero(grid == 0)
        target = gamma * float(np.max(q[key][legal]))
    return errs


def policy_entropy(q_values: np.ndarray, legal: np.ndarray,
                   tau: float = 0.3) -> float:
    """Q 表导出的策略熵：softmax(Q/τ) 在合法动作上的 Shannon 熵。"""
    z = q_values[legal] / tau
    z = z - z.max()
    p = np.exp(z)
    p = p / p.sum()
    return float(-(p * np.log(p + 1e-12)).sum())


def run_episode(q: dict, opponent, eps: float, rng: np.random.Generator,
                gamma: float, lr: float,
                size: int = SIZE, win_len: int = WIN_LEN) -> dict:
    """学员固定执黑先行打一局，终局后回填；返回单局指标。"""
    board = Board(size=size, win_len=win_len)
    trajectory: list[tuple[bytes, int]] = []
    entropies: list[float] = []
    while not board.game_over:
        if board.current_player == BLACK:
            key = state_key(board)
            q.setdefault(key, np.zeros(size * size))
            action = select_action(q, board, eps, rng)
            trajectory.append((key, action))
            entropies.append(policy_entropy(q[key], _legal_actions(board)))
        else:
            action = opponent(board, rng)
        row, col = board.action_to_move(action)
        board.play(row, col)
    reward = {BLACK: 1.0, WHITE: -1.0, DRAW: 0.0}[board.winner]
    td_errors = td_backfill(q, trajectory, reward, gamma, lr)
    return {
        "winner": int(board.winner),
        "steps": len(board.history),
        "td_error": float(np.mean(td_errors)),
        "entropy": float(np.mean(entropies)),
    }


def train(opponent: str, episodes: int,
          eps_start: float = 1.0, eps_end: float = 0.05,
          eps_decay_episodes: int = 3000,
          gamma: float = 0.99, lr: float = 0.5,
          seed: int | None = None,
          size: int = SIZE, win_len: int = WIN_LEN
          ) -> tuple[dict[bytes, np.ndarray], list[dict]]:
    """ε 线性退火跑 episodes 局；返回 (Q 表, 逐局 metrics 行)。"""
    rng = np.random.default_rng(seed)
    opp = OPPONENTS[opponent]
    q: dict[bytes, np.ndarray] = {}
    rows: list[dict] = []
    for episode in range(episodes):
        frac = min(1.0, episode / max(1, eps_decay_episodes))
        eps = eps_start + (eps_end - eps_start) * frac
        info = run_episode(q, opp, eps, rng, gamma, lr, size, win_len)
        rows.append({"episode": episode, **info, "eps": eps})
    return q, rows


def save_qtable(q: dict, path: str, meta: dict) -> None:
    payload = {
        "meta": meta,
        "states": {base64.b64encode(k).decode("ascii"): v.tolist()
                   for k, v in q.items()},
    }
    with open(path, "w", encoding="utf-8") as f:
        json.dump(payload, f)


def load_qtable(path: str) -> tuple[dict[bytes, np.ndarray], dict]:
    with open(path, encoding="utf-8") as f:
        payload = json.load(f)
    q = {base64.b64decode(k): np.asarray(v, dtype=np.float64)
         for k, v in payload["states"].items()}
    return q, payload["meta"]


def sliding_win_rate(rows: list[dict], window: int = 100) -> list[float]:
    """学员（黑）胜率的滑动平均；窗口不足时按已有局数平均。"""
    out = []
    for i in range(len(rows)):
        chunk = rows[max(0, i - window + 1): i + 1]
        out.append(sum(r["winner"] == BLACK for r in chunk) / len(chunk))
    return out
```

- [ ] **Step 4: 测试通过**

Run: `python -m pytest tests/test_tabular.py -v`
Expected: 10 passed

- [ ] **Step 5: 写训练脚本与绘图脚本**

创建 `scripts/train_tabular.py`：

```python
"""课 01 · E0：表格 Q-learning 训练入口（headless）。

用法：
    python scripts/train_tabular.py --opponent random --episodes 5000 \
        --eps-start 1.0 --eps-end 0.05 --eps-decay-episodes 3000 \
        --gamma 0.99 --lr 0.5 --exp-id e0-stage1

输出 runs/<exp-id>/：config.json + metrics.jsonl + curve.png + qtable.json
"""

import argparse
import json
import pathlib

import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt

from gomoku.rl.tabular import save_qtable, sliding_win_rate, train


def plot_curves(rows, out_path: str, window: int = 100) -> None:
    episodes = [r["episode"] for r in rows]
    fig, axes = plt.subplots(2, 2, figsize=(12, 8))
    axes[0, 0].plot(episodes, sliding_win_rate(rows, window))
    axes[0, 0].set_title(f"win rate (sliding {window})")
    axes[0, 1].plot(episodes, [r["steps"] for r in rows])
    axes[0, 1].set_title("episode length")
    axes[1, 0].plot(episodes, [r["td_error"] for r in rows])
    axes[1, 0].set_title("TD error (mean |delta|)")
    axes[1, 1].plot(episodes, [r["entropy"] for r in rows])
    axes[1, 1].set_title("policy entropy")
    for ax in axes.flat:
        ax.set_xlabel("episode")
    fig.tight_layout()
    fig.savefig(out_path, dpi=120)
    plt.close(fig)


def main() -> None:
    parser = argparse.ArgumentParser(description="E0 表格 Q-learning 训练")
    parser.add_argument("--opponent", choices=["random", "blocker"],
                        default="random")
    parser.add_argument("--episodes", type=int, default=5000)
    parser.add_argument("--eps-start", type=float, default=1.0)
    parser.add_argument("--eps-end", type=float, default=0.05)
    parser.add_argument("--eps-decay-episodes", type=int, default=3000)
    parser.add_argument("--gamma", type=float, default=0.99)
    parser.add_argument("--lr", type=float, default=0.5)
    parser.add_argument("--seed", type=int, default=0)
    parser.add_argument("--exp-id", default="e0-stage1")
    args = parser.parse_args()

    out = pathlib.Path("runs") / args.exp_id
    out.mkdir(parents=True, exist_ok=True)
    (out / "config.json").write_text(
        json.dumps(vars(args), ensure_ascii=False, indent=2), encoding="utf-8")

    q, rows = train(opponent=args.opponent, episodes=args.episodes,
                    eps_start=args.eps_start, eps_end=args.eps_end,
                    eps_decay_episodes=args.eps_decay_episodes,
                    gamma=args.gamma, lr=args.lr, seed=args.seed)

    with open(out / "metrics.jsonl", "w", encoding="utf-8") as f:
        for r in rows:
            f.write(json.dumps(r) + "\n")
    plot_curves(rows, str(out / "curve.png"))
    save_qtable(q, str(out / "qtable.json"), meta={
        "size": 3, "win_len": 3, "opponent": args.opponent,
        "episodes": args.episodes, "gamma": args.gamma, "lr": args.lr,
    })
    print(f"done: {args.exp_id}  "
          f"final win rate (last 100) = {sliding_win_rate(rows)[-1]:.2f}")


if __name__ == "__main__":
    main()
```

创建 `scripts/plot_qtable.py`：

```python
"""课 01 · E0：Q 表热力图快照。

用法：python scripts/plot_qtable.py runs/e0-stage1/qtable.json

打印并绘制两个局面（空盘·黑先 / 双威胁局面·黑先）的 3×3 Q 值，
输出 PNG 与 qtable.json 同目录（qtable-snapshots.png）。
局面不在 Q 表中时打印警告并以全 0 兜底。
"""

import base64
import json
import pathlib
import sys

import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt
import numpy as np

from gomoku.core import BLACK


def _empty_key() -> bytes:
    return np.zeros((3, 3), dtype=np.int8).tobytes() + bytes([BLACK])


def _double_threat_key() -> bytes:
    """黑 (0,0)(2,0)(1,1)、白 (0,1)(1,0)、黑先：两个威胁点 (0,2) 与 (2,2)。"""
    grid = np.zeros((3, 3), dtype=np.int8)
    for r, c in [(0, 0), (2, 0), (1, 1)]:
        grid[r, c] = 1
    for r, c in [(0, 1), (1, 0)]:
        grid[r, c] = 2
    return grid.tobytes() + bytes([BLACK])


def _draw(ax, values, title: str) -> None:
    ax.imshow(values.reshape(3, 3), vmin=-1, vmax=1, cmap="RdYlGn")
    for r in range(3):
        for c in range(3):
            ax.text(c, r, f"{values[r * 3 + c]:.2f}",
                    ha="center", va="center", fontsize=12)
    ax.set_title(title)
    ax.set_xticks(range(3))
    ax.set_yticks(range(3))


def main() -> None:
    path = pathlib.Path(sys.argv[1])
    payload = json.loads(path.read_text(encoding="utf-8"))
    q = {base64.b64decode(k): np.asarray(v)
         for k, v in payload["states"].items()}
    snapshots = [
        ("开局：空盘·黑先", _empty_key()),
        ("双威胁局面·黑先", _double_threat_key()),
    ]
    fig, axes = plt.subplots(1, 2, figsize=(9, 4.5))
    for ax, (title, key) in zip(axes, snapshots):
        values = q.get(key)
        if values is None:
            print(f"[警告] Q 表中没有该局面：{title}")
            values = np.zeros(9)
        print(f"\n{title}\n{values.reshape(3, 3)}")
        _draw(ax, values, title)
    fig.tight_layout()
    out = path.parent / "qtable-snapshots.png"
    fig.savefig(out, dpi=120)
    print(f"\nsaved: {out}")


if __name__ == "__main__":
    main()
```

`pyproject.toml` 的 `dependencies` 数组末尾加一行：

```toml
    "matplotlib>=3.8",
```

- [ ] **Step 6: 冒烟运行训练脚本**

Run: `python scripts/train_tabular.py --opponent random --episodes 200 --eps-end 0.2 --eps-decay-episodes 100 --exp-id e0-smoke && python scripts/plot_qtable.py runs/e0-smoke/qtable.json`
Expected: 两个命令正常结束；`runs/e0-smoke/` 下有 config.json、metrics.jsonl、curve.png、qtable.json、qtable-snapshots.png；打印 "[警告] Q 表中没有该局面：双威胁局面·黑先" 属正常（200 局训练覆盖面小）。

- [ ] **Step 7: 全量测试通过**

Run: `python -m pytest`
Expected: 59 passed

- [ ] **Step 8: Commit**

```bash
git add src/gomoku/rl scripts/train_tabular.py scripts/plot_qtable.py tests/test_tabular.py pyproject.toml
git commit -m "feat: 表格 Q-learning 库 + 训练/热力图脚本（E0）"
```

---

### Task 3: QTablePlayer

**Files:**
- Create: `src/gomoku/players/qtable.py`
- Modify: `src/gomoku/players/__init__.py`
- Test: `tests/test_qtable_player.py`（新文件）

**Interfaces:**
- Consumes: `Player`（`players/base.py`，`select_move(board) -> int`）、`load_qtable` / `state_key`（Task 2）。
- Produces: `QTablePlayer(qtable_path: str, name: str = "qtable-3x3")`，Task 4 的 `_make_player` 使用。

- [ ] **Step 1: 写失败测试**

创建 `tests/test_qtable_player.py`：

```python
import base64
import json

from gomoku.core import Board
from gomoku.players.qtable import QTablePlayer


def _write_qtable(tmp_path, states) -> str:
    path = tmp_path / "qtable.json"
    path.write_text(json.dumps({"meta": {}, "states": states}),
                    encoding="utf-8")
    return str(path)


def test_select_move_argmax_masks_occupied(tmp_path):
    b = Board(size=3, win_len=3)
    b.play(1, 1)          # 黑
    b.play(0, 0)          # 白
    b.play(2, 2)          # 黑 → 轮白，查表的应是白方 Q
    key = b.grid.tobytes() + bytes([b.current_player])
    values = [0.0] * 9
    values[1] = 5.0       # (0,1) 最优；(0,0)(2,2) 已占会被 mask
    path = _write_qtable(tmp_path,
                         {base64.b64encode(key).decode(): values})
    p = QTablePlayer(path)
    assert b.action_to_move(p.select_move(b)) == (0, 1)


def test_select_move_unknown_state_random_legal(tmp_path):
    p = QTablePlayer(_write_qtable(tmp_path, {}))
    b = Board(size=3, win_len=3)
    r, c = b.action_to_move(p.select_move(b))
    assert b.grid[r, c] == 0
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_qtable_player.py -v`
Expected: FAIL，`ModuleNotFoundError: No module named 'gomoku.players.qtable'`

- [ ] **Step 3: 实现**

创建 `src/gomoku/players/qtable.py`：

```python
"""QTablePlayer：加载表格 Q-learning 产出的 qtable.json 参与对战。"""

import random

import numpy as np

from gomoku.players.base import Player
from gomoku.rl.tabular import load_qtable, state_key


class QTablePlayer(Player):
    """查表 + 合法动作 mask + argmax；查不到的局面 → 随机合法位。"""

    def __init__(self, qtable_path: str, name: str = "qtable-3x3"):
        self.name = name
        self.q, self.meta = load_qtable(qtable_path)

    def select_move(self, board) -> int:
        legal = np.flatnonzero(board.valid_moves())
        qs = self.q.get(state_key(board))
        if qs is None:
            return int(random.choice(legal.tolist()))
        return int(legal[np.argmax(qs[legal])])
```

`src/gomoku/players/__init__.py` 改为：

```python
from gomoku.players.base import Player
from gomoku.players.human import HumanPlayer
from gomoku.players.minimax import MinimaxPlayer
from gomoku.players.nn import NNPlayer
from gomoku.players.qtable import QTablePlayer

__all__ = ["Player", "HumanPlayer", "MinimaxPlayer", "NNPlayer", "QTablePlayer"]
```

- [ ] **Step 4: 测试通过 + 全量**

Run: `python -m pytest`
Expected: 61 passed

- [ ] **Step 5: Commit**

```bash
git add src/gomoku/players/qtable.py src/gomoku/players/__init__.py tests/test_qtable_player.py
git commit -m "feat: QTablePlayer 接入 Player 统一接口"
```

---

### Task 4: server 集成（size 透传 + qtable 分支 + DB size 列）

**Files:**
- Modify: `src/gomoku/db.py`
- Modify: `src/gomoku/sgf.py`
- Modify: `src/gomoku/server/app.py`
- Test: `tests/test_db.py`、`tests/test_server.py`（各追加）

**Interfaces:**
- Consumes: `Board(size, win_len)`（Task 1）、`QTablePlayer`（Task 3）。
- Produces:
  - `create_game(conn, black_name, white_name, size: int = 15) -> int`；games 表新增 `size` 列（旧库自动 ALTER 迁移，默认 15）。
  - `build_sgf(black_name, white_name, result, moves, size: int = 15) -> str`
  - `POST /api/new` 请求体新增 `size: int = 15`（仅接受 3 或 15）；响应与 `/api/games/{id}/record` 响应新增 `"size"` 字段；`_make_player("qtable")` 从模块常量 `app.QTABLE_PATH`（`runs/e0-stage2/qtable.json`）加载。

- [ ] **Step 1: 写失败测试**

`tests/test_db.py` 末尾追加（若文件顶部未 `import sqlite3`，在测试函数内局部导入即可）：

```python
def test_migrates_missing_size_column(tmp_path):
    import sqlite3
    path = str(tmp_path / "old.db")
    raw = sqlite3.connect(path)
    raw.executescript(
        "CREATE TABLE games ("
        "id INTEGER PRIMARY KEY AUTOINCREMENT,"
        "started_at TEXT NOT NULL DEFAULT (datetime('now', 'localtime')),"
        "black_name TEXT NOT NULL, white_name TEXT NOT NULL,"
        "result TEXT NOT NULL DEFAULT 'ongoing',"
        "total_moves INTEGER NOT NULL DEFAULT 0);"
    )
    raw.commit()
    raw.close()
    conn = connect(path)
    conn.execute("INSERT INTO games (black_name, white_name) VALUES ('a', 'b')")
    (size,) = conn.execute("SELECT size FROM games").fetchone()
    assert size == 15
    conn.close()


def test_create_game_with_size(tmp_path):
    conn = connect(str(tmp_path / "t.db"))
    gid = create_game(conn, "a", "b", size=3)
    (size,) = conn.execute("SELECT size FROM games WHERE id = ?", (gid,)).fetchone()
    assert size == 3
    conn.close()
```

`tests/test_server.py` 末尾追加（`json` 若未导入则在文件头补 `import json`）：

```python
def test_new_game_3x3():
    r = client.post("/api/new", json={"black": "human", "white": "human", "size": 3})
    assert r.status_code == 200
    s = r.json()
    assert s["size"] == 3 and len(s["board"]) == 3


def test_new_game_rejects_bad_size():
    r = client.post("/api/new", json={"black": "human", "white": "human", "size": 4})
    assert r.status_code == 400


def test_qtable_requires_3x3():
    r = client.post("/api/new", json={"black": "qtable", "white": "human", "size": 15})
    assert r.status_code == 400


def test_qtable_first_move(tmp_path, monkeypatch):
    path = tmp_path / "qtable.json"
    path.write_text(json.dumps({"meta": {}, "states": {}}), encoding="utf-8")
    monkeypatch.setattr(app_module, "QTABLE_PATH", str(path))
    r = client.post("/api/new", json={"black": "qtable", "white": "human", "size": 3})
    assert r.status_code == 200
    s = r.json()
    assert sum(v != 0 for row in s["board"] for v in row) == 1   # AI 执黑先走一手
    assert s["current_player"] == 2


def test_qtable_missing_file_400(tmp_path, monkeypatch):
    monkeypatch.setattr(app_module, "QTABLE_PATH", str(tmp_path / "nope.json"))
    r = client.post("/api/new", json={"black": "qtable", "white": "human", "size": 3})
    assert r.status_code == 400


def test_3x3_record_and_sgf():
    r = client.post("/api/new", json={"black": "human", "white": "human", "size": 3})
    gid = r.json()["game_id"]
    for row, col in [(0, 0), (1, 0), (0, 1), (1, 1), (0, 2)]:
        client.post("/api/move", json={"game_id": gid, "row": row, "col": col})
    rec = client.get(f"/api/games/{gid}/record").json()
    assert rec["size"] == 3 and rec["result"] == "black_win"
    assert len(rec["win_line"]) == 3
    sgf = client.get(f"/api/games/{gid}/sgf")
    assert sgf.status_code == 200 and "SZ[3]" in sgf.text
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_db.py tests/test_server.py -v`
Expected: 新增 8 个测试 FAIL（size 列不存在 / qtable 未实现 / record 无 size 字段）

- [ ] **Step 3: 实现 db.py**

`SCHEMA` 中 games 表定义在 `total_moves` 行后加一行：

```sql
    size INTEGER NOT NULL DEFAULT 15
```

`connect()` 中 `conn.executescript(SCHEMA)` 之后、`conn.commit()` 之前加迁移：

```python
    cols = {r["name"] for r in conn.execute("PRAGMA table_info(games)")}
    if "size" not in cols:
        conn.execute("ALTER TABLE games ADD COLUMN size INTEGER NOT NULL DEFAULT 15")
```

`create_game` 改为：

```python
def create_game(conn: sqlite3.Connection, black_name: str, white_name: str,
                size: int = 15) -> int:
    cur = conn.execute(
        "INSERT INTO games (black_name, white_name, size) VALUES (?, ?, ?)",
        (black_name, white_name, size),
    )
    conn.commit()
    return cur.lastrowid
```

- [ ] **Step 4: 实现 sgf.py**

`build_sgf` 签名与 SZ 行改为：

```python
def build_sgf(black_name: str, white_name: str, result: str,
              moves: list[tuple[int, int, int]], size: int = 15) -> str:
    """moves: [(player, row, col), ...]（Board.history / moves 表同构）。"""
    parts = [f"(;FF[4]SZ[{size}]PB[{black_name}]PW[{white_name}]"]
```

（其余不动；模块 docstring 中 "a–o" 仍在 3×3 下自然成立 a–c。）

- [ ] **Step 5: 实现 app.py**

imports 增加：

```python
from gomoku.players.minimax import MinimaxPlayer
from gomoku.players.qtable import QTablePlayer
```

（MinimaxPlayer 原本已导入；只需补 QTablePlayer 一行。）

模块常量（`_RESULT` 定义之前）：

```python
QTABLE_PATH = "runs/e0-stage2/qtable.json"
```

`_make_player` 增加 qtable 分支（在 minimax 分支后、raise 之前）：

```python
    if kind == "qtable":
        p = pathlib.Path(QTABLE_PATH)
        if not p.exists():
            raise ValueError(
                f"Q 表不存在：{QTABLE_PATH}（先运行 scripts/train_tabular.py 训练）")
        return QTablePlayer(str(p))
```

`NewGameReq` 增加：

```python
class NewGameReq(BaseModel):
    black: str = "human"
    white: str = "minimax-hard"
    size: int = 15
```

`new_game` 开头（try 之前）加校验：

```python
    if req.size not in (3, 15):
        raise HTTPException(status_code=400, detail="size 仅支持 3 或 15")
    if "qtable" in (req.black, req.white) and req.size != 3:
        raise HTTPException(status_code=400, detail="qtable 仅支持 3×3 对局")
```

`new_game` 内 `board = Board()` 改为：

```python
        board = Board(size=req.size, win_len=3 if req.size == 3 else 5)
```

`create_game(conn, req.black, req.white)` 改为 `create_game(conn, req.black, req.white, size=req.size)`

`_state` 返回 dict 增加（`"game_id"` 行后）：

```python
        "size": b.size,
```

`game_record` 中 `board = Board()` 改为：

```python
    size = row["size"] or 15
    board = Board(size=size, win_len=3 if size == 3 else 5)
```

返回 dict 增加 `"size": size,`（`"game_id"` 行后）。

`game_sgf` 中 `content = build_sgf(...)` 改为：

```python
    content = build_sgf(
        row["black_name"], row["white_name"], row["result"],
        [(m["player"], m["row"], m["col"]) for m in move_rows],
        size=row["size"] or 15,
    )
```

- [ ] **Step 6: 全量测试通过**

Run: `python -m pytest`
Expected: 69 passed

- [ ] **Step 7: Commit**

```bash
git add src/gomoku/db.py src/gomoku/sgf.py src/gomoku/server/app.py tests/test_db.py tests/test_server.py
git commit -m "feat: server 支持棋盘尺寸与 qtable 玩家，DB/SGF 随 size"
```

---

### Task 5: 前端（Board 参数化 + 「九宫格·Q表」入口 + 回放兼容）

**Files:**
- Modify: `frontend/src/Board.jsx`
- Modify: `frontend/src/App.jsx`
- Modify: `frontend/src/Replay.jsx`
- Modify: `frontend/src/api.js`
- Modify: `frontend/src/index.css`（追加一条规则）

**Interfaces:**
- Consumes: `POST /api/new` 的 `size` 参数与响应 `size` 字段（Task 4）；`record.size` 字段（Task 4）。
- Produces: `Board` 新增 prop `size`（默认 15）；`api.newGame(black, white, size = 15)`。

- [ ] **Step 1: Board.jsx 参数化（整文件替换）**

```jsx
export default function Board({ grid, lastMove, winLine, onPlay, locked, size = 15 }) {
  const N = size;
  const CELL = size === 3 ? 64 : 36;
  const PAD = size === 3 ? 40 : 24;
  const SIZE = PAD * 2 + CELL * (N - 1);
  const stars = N === 15 ? [[3, 3], [3, 11], [11, 3], [11, 11], [7, 7]] : [];

  const xyToRC = (x, y) => {
    const c = Math.round((x - PAD) / CELL);
    const r = Math.round((y - PAD) / CELL);
    if (r < 0 || r >= N || c < 0 || c >= N) return null;
    const px = PAD + c * CELL, py = PAD + r * CELL;
    if (Math.hypot(x - px, y - py) > CELL * 0.45) return null;
    return [r, c];
  };

  const winIndex = new Map();
  if (winLine) {
    winLine.forEach(([r, c], i) => winIndex.set(r + "-" + c, i));
  }

  const handleClick = (e) => {
    if (locked) return;
    const rect = e.currentTarget.getBoundingClientRect();
    const rc = xyToRC(e.clientX - rect.left, e.clientY - rect.top);
    if (rc && grid[rc[0]][rc[1]] === 0) onPlay(rc[0], rc[1]);
  };

  const lines = [];
  for (let i = 0; i < N; i++) {
    lines.push(
      <line key={"h" + i} x1={PAD} y1={PAD + i * CELL} x2={SIZE - PAD} y2={PAD + i * CELL}
            stroke="#8a6d3b" strokeWidth="1" />,
      <line key={"v" + i} x1={PAD + i * CELL} y1={PAD} x2={PAD + i * CELL} y2={SIZE - PAD}
            stroke="#8a6d3b" strokeWidth="1" />
    );
  }

  return (
    <svg data-testid="board" width={SIZE} height={SIZE} onClick={handleClick}
         style={{ background: "#dcb35c", cursor: locked ? "default" : "pointer",
                  borderRadius: 4 }}>
      {lines}
      {stars.map(([r, c]) => (
        <circle key={"s" + r + "-" + c} cx={PAD + c * CELL} cy={PAD + r * CELL}
                r="3" fill="#5a4632" />
      ))}
      {grid.map((row, r) =>
        row.map((v, c) =>
          v === 0 ? null : (
            (() => {
              const key = r + "-" + c;
              const winIdx = winIndex.get(key);
              return (
                <circle key={key} cx={PAD + c * CELL} cy={PAD + r * CELL}
                        r={CELL * 0.42}
                        className={winIdx !== undefined ? "stone-win" : undefined}
                        opacity={winIndex.size && winIdx === undefined ? 0.45 : 1}
                        style={winIdx !== undefined
                          ? { animationDelay: `${winIdx * 0.15}s` } : undefined}
                        fill={v === 1 ? "#111" : "#fff"} stroke="#555" />
              );
            })()
          )
        )
      )}
      {lastMove && grid[lastMove[0]][lastMove[1]] !== 0 && (
        <circle cx={PAD + lastMove[1] * CELL} cy={PAD + lastMove[0] * CELL}
                r="4" fill="#e33" />
      )}
    </svg>
  );
}
```

- [ ] **Step 2: api.js 透传 size**

`newGame` 一行改为：

```js
  newGame: (black, white, size = 15) => post("/api/new", { black, white, size }),
```

- [ ] **Step 3: App.jsx 增加「九宫格·Q表」模式**

`labelOf` 改为：

```jsx
function labelOf(kind) {
  return OPTIONS.find((o) => o.value === kind)?.label
    ?? ({ qtable: "九宫格·Q表" }[kind] ?? kind);
}
```

组件内 `config` state 声明后增加：

```jsx
  const [mode, setMode] = useState("standard");   // standard | qtable3
```

`start` 改为：

```jsx
  const start = async () => {
    setBusy(true);
    try {
      if (mode === "qtable3") {
        setConfig({ black: "qtable", white: "human" });
        setGame(await api.newGame("qtable", "human", 3));
      } else {
        setGame(await api.newGame(config.black, config.white, 15));
      }
      setView("game");
    } catch (e) { showToast(e.message); }
    finally { setBusy(false); }
  };
```

配置页（`if (!game)` 分支）在两个 select 的 `{["black", "white"].map(...)}` 之前插入模式行，并把 select 区域改为仅 standard 模式显示：

```jsx
        <div className="mode-row">
          <label>
            <input type="radio" name="mode" checked={mode === "standard"}
                   onChange={() => {
                     setMode("standard");
                     if (config.black === "qtable") {
                       setConfig({ black: "human", white: "minimax-hard" });
                     }
                   }} />
            标准 15×15
          </label>
          <label>
            <input type="radio" name="mode" checked={mode === "qtable3"}
                   onChange={() => setMode("qtable3")} />
            九宫格·Q表（3×3，E0）
          </label>
        </div>
        {mode === "qtable3" && <p>黑方：九宫格·Q表 AI　白方：玩家</p>}
        {mode === "standard" && ["black", "white"].map((side) => (
          ...原有 label + select JSX 原样保留...
        ))}
```

对局视图 `Board` 调用增加 `size` prop：

```jsx
      <Board grid={game.board} lastMove={game.last_move} winLine={game.win_line}
             onPlay={play} locked={busy || game.game_over || !humanTurn}
             size={config.black === "qtable" ? 3 : 15} />
```

（qtable3 模式下 `config.black` 在 start 时已置为 `"qtable"`，AI 回合 effect、悔棋 canUndo、status 标签全部复用现有 config 逻辑，无需额外改动。）

- [ ] **Step 4: Replay.jsx 兼容 3×3**

`buildGrid` 改为：

```jsx
function buildGrid(moves, ply, size) {
  const grid = Array.from({ length: size }, () => Array(size).fill(0));
  for (let i = 0; i < ply; i++) {
    const [p, r, c] = moves[i];
    grid[r][c] = p;
  }
  return grid;
}
```

组件内 `const { moves, win_line, black_name, white_name, result } = record;` 后加：

```jsx
  const size = record.size ?? 15;
```

`<Board ...>` 调用改为：

```jsx
      <Board grid={buildGrid(moves, ply, size)} lastMove={last} winLine={showWin}
             onPlay={() => {}} locked size={size} />
```

- [ ] **Step 5: index.css 追加**

```css
.mode-row { display: flex; gap: 16px; margin: 8px 0; }
```

- [ ] **Step 6: 构建验证**

Run: `cd frontend && npm run build`
Expected: vite build 成功，无报错

- [ ] **Step 7: Commit**

```bash
git add frontend/src/Board.jsx frontend/src/App.jsx frontend/src/Replay.jsx frontend/src/api.js frontend/src/index.css
git commit -m "feat: 前端支持 3×3 九宫格与 Q表 AI 入口"
```

---

### Task 6: 训练两阶段 + 实验笔记 + 网页验收

**Files:**
- Create: `docs/course/01-dqn/e0-tabular.md`
- Create: `docs/course/01-dqn/assets/`（曲线与热力图 PNG）
- Modify: `.gitignore`（追加 `runs/`）

**Interfaces:**
- Consumes: Task 2 的训练/绘图脚本；Task 4 的 server；Task 5 的前端。
- Produces: `runs/e0-stage1/qtable.json` 与 `runs/e0-stage2/qtable.json`（后者即 server 的 `QTABLE_PATH`）；五段式实验笔记。

本任务无新代码，验收以真实产物为准。

- [ ] **Step 1: gitignore 训练产物**

`.gitignore` 追加一行：

```
runs/
```

- [ ] **Step 2: 阶段 1 训练（vs random）**

Run: `python scripts/train_tabular.py --opponent random --episodes 5000 --eps-start 1.0 --eps-end 0.05 --eps-decay-episodes 3000 --gamma 0.99 --lr 0.5 --exp-id e0-stage1`
Expected: 末行打印 `final win rate (last 100) ≈ 0.97`（seed=0 实测 0.97，≥0.90 达标）；`runs/e0-stage1/` 四件产物齐全。

- [ ] **Step 3: 阶段 2 训练（vs blocker）**

Run: `python scripts/train_tabular.py --opponent blocker --episodes 10000 --eps-start 1.0 --eps-end 0.05 --eps-decay-episodes 6000 --gamma 0.99 --lr 0.5 --exp-id e0-stage2`
Expected: 末行打印 `final win rate (last 100) ≈ 0.89`（seed=0 实测 0.89，显著上升达标）。若 < 0.6，重跑一次 `--episodes 30000`（blocker 会堵单威胁，学员必须靠双威胁取胜，收敛更慢属预期）。

- [ ] **Step 4: Q 表热力图**

Run: `python scripts/plot_qtable.py runs/e0-stage1/qtable.json && python scripts/plot_qtable.py runs/e0-stage2/qtable.json`
Expected: 阶段 1 开局快照中天元 (1,1) Q 值最高（实测 0.966）；双威胁快照若打印"[警告] Q 表中没有该局面"属正常（该精确局面训练中未必出现），如实记入笔记。

- [ ] **Step 5: 产物归档**

```bash
mkdir -p docs/course/01-dqn/assets
cp runs/e0-stage1/curve.png docs/course/01-dqn/assets/e0-stage1-curve.png
cp runs/e0-stage2/curve.png docs/course/01-dqn/assets/e0-stage2-curve.png
cp runs/e0-stage1/qtable-snapshots.png docs/course/01-dqn/assets/e0-stage1-qtable.png
cp runs/e0-stage2/qtable-snapshots.png docs/course/01-dqn/assets/e0-stage2-qtable.png
```

- [ ] **Step 6: 写实验笔记（五段式 × 两阶段）**

创建 `docs/course/01-dqn/e0-tabular.md`，结构如下。**所有数字必须取自实际运行的 metrics.jsonl 与打印输出，不许编造**；曲线图引用 assets 下的 PNG：

```markdown
# E0 · 表格 Q-learning（3×3 连三制）——DQN 概念验收

> 课 01 第一刀。目的：把 ε-greedy、TD 更新、稀疏奖励回填整条环路
> 在亲眼看得清的 3×3 上跑通，再让训练成果进网页对战。

## 问题

（为什么先做 3×3 表格：机制 bug 在 3×3 五分钟暴露，在 15×15 要烧几小时；
Q 表 dict[bytes, 9 维向量] 每一行都能打印出来看。）

## 设置

（训练循环要点：状态键 = grid 字节 + 行棋方；ε 1.0→0.05 线性退火；
lr 0.5；γ 0.99；学员执黑；对手 random / blocker（两连必堵否则随机）；
终局统一回填 +1/-1/0。两个阶段各自给出 runs/e0-*/config.json 的实际超参表。）

## 阶段 1 vs random

### 你应该会看到

![stage1 曲线](assets/e0-stage1-curve.png)

（四条曲线逐一描述：胜率从 ~50% 爬到 X%、平均局长先升后降、
TD error 大→收敛变小、策略熵缓慢下降。**填实测数字**。）

![stage1 Q表](assets/e0-stage1-qtable.png)

（开局快照：天元 Q 值最高 X——3×3 连三先手最优开局就是中心，
Q 表自己"发现"了这一点。）

### 反例

（如双威胁快照缺失、熵曾短暂回升、某段胜率回落等现象，如实记录并解释。）

## 阶段 2 vs blocker

### 你应该会看到

![stage2 曲线](assets/e0-stage2-curve.png)

（blocker 会堵一切单威胁 → 学员唯一赢法是制造"堵一漏一"的双威胁。
胜率曲线前期贴地、后期爬升至 X%。**填实测数字**。）

![stage2 Q表](assets/e0-stage2-qtable.png)

### 反例

（同上，如实记录。）

## 结论

（验收对照 spec 五条：① 阶段1 胜率 90%+ ✓/✗ ② 阶段2 双威胁取胜 ✓/✗
③ 热力图可见天元/双威胁价值结构 ✓/✗ ④ 网页对战 AI 下天元、能赢会堵 ✓/✗
⑤ 全量 pytest + npm run build ✓。以及一句话：表格的极限在哪里——
状态数随棋盘指数爆炸，15×15 有 3^225 个局面，这就是 E1 换函数近似的理由。）
```

- [ ] **Step 7: 全量测试**

Run: `python -m pytest`
Expected: 69 passed（本任务不改代码，应保持绿）

- [ ] **Step 8: Commit**

```bash
git add docs/course/01-dqn .gitignore
git commit -m "docs: E0 实验笔记（两阶段曲线+Q表快照，五段式）"
```

- [ ] **Step 9: 网页验收（人工，由控制器/用户执行）**

1. 确认 `runs/e0-stage2/qtable.json` 存在；
2. `npm run build` 后启动 `uvicorn`，浏览器打开配置页；
3. 选「九宫格·Q表（3×3）」开始对局：AI 黑先手应下天元附近，能赢会堵；
4. 悔棋（白方回合悔一轮两子）、棋谱下载 SGF（SZ[3]）、历史回放均正常。

---

## Self-Review 记录

- **Spec 覆盖**：§1 Board 泛化→Task 1；§2 Q表与训练脚本→Task 2；§3 观测层→Task 2（metrics/曲线）+ Task 6（运行与笔记）；§4 网页接入→Task 3/4/5；§5 验收→Task 6 Step 2/3/4/9 + 各任务全量测试；"明确不做"未混入。
- **占位符扫描**：实验笔记中的 X 为"填实测数字"的刻意指令（数据依赖任务，要求取自真实 metrics），非 TBD。
- **类型一致性**：`load_qtable` 返回 `(dict[bytes, ndarray], dict)` 与 QTablePlayer 用法一致；`create_game(..., size=)` 与 server 调用一致；`record.size`/`state.size` 前后端一致；`sliding_win_rate(rows)[-1]` 在脚本与测试中一致。
- **已验证数值**：seed=0 模拟实测 smoke 0.75 / stage1 0.97（天元 0.966 最高）/ stage2 0.89，测试阈值均留有余量。
